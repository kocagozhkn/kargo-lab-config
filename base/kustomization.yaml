apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - nginx-status-config.yaml

commonLabels:
  app.kubernetes.io/name: nginx
  app.kubernetes.io/part-of: kargo-lab
